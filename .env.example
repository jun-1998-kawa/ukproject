# Kendo Club Analysis Engine - Environment Variables

# ============================================
# AI Configuration (Amazon Bedrock)
# ============================================

# Bedrock model ID (required for AI features)
# Anthropic Claude 3.5 Sonnet (recommended)
AI_MODEL_ID=anthropic.claude-3-5-sonnet-20240620-v1:0

# Bedrock region (optional, defaults to Lambda's AWS_REGION)
AI_BEDROCK_REGION=us-east-1

# ============================================
# System Prompt Configuration
# Priority: S3 → Environment Variable → Default
# ============================================

# Option 1: S3 (recommended for long prompts)
# S3 bucket name for storing system prompts
AI_PROMPT_S3_BUCKET=kendo-ai-prompts-YOUR-ACCOUNT-ID

# S3 object key (path to prompt file)
AI_PROMPT_S3_KEY=prompts/system-prompt.txt

# Option 2: Environment Variable (for short prompts < 4KB)
# Uncomment to use (overrides default, but S3 takes priority if configured)
# AI_SYSTEM_PROMPT="あなたは大学剣道部のアナリティクスコーチです。提供された統計データのみに基づいて分析を行い、全ての数値に根拠を明記してください。日本語で回答してください。"

# ============================================
# Seed Scripts Configuration
# ============================================

# GraphQL API endpoint (from amplify_outputs.json)
SEED_API_URL=https://YOUR-APPSYNC-ENDPOINT.appsync-api.us-east-1.amazonaws.com/graphql

# Cognito ID token for authentication
# Get this from your app's sign-in flow or Amplify console
SEED_AUTH_TOKEN=eyJraWQiOiI...

# Authentication mode for seed scripts
# Options: "COGNITO" (default), "IAM"
SEED_AUTH_MODE=COGNITO

# ============================================
# Deployment Configuration
# ============================================

# AWS region for deployment
# AWS_REGION=us-east-1

# ============================================
# Development Configuration
# ============================================

# Enable debug logging (optional)
# DEBUG=true

# ============================================
# Notes
# ============================================
#
# 1. Copy this file to `.env` and fill in your actual values
# 2. NEVER commit `.env` to version control (it's in .gitignore)
# 3. For S3 prompts, create the bucket first:
#    aws s3 mb s3://kendo-ai-prompts-YOUR-ACCOUNT-ID
# 4. Upload your prompt file:
#    aws s3 cp system-prompt.txt s3://kendo-ai-prompts-YOUR-ACCOUNT-ID/prompts/system-prompt.txt
# 5. See docs/AI_SYSTEM_PROMPT_SETUP.md for detailed setup instructions
